<div class="container">
    <div class="text-center m-3 p-2"><strong>Bikes located in <%= @query%></strong></div>
  <div class="row m-3">
    <% @bicycles.each do |bicycle|  %>
    <%= render "card", bicycle: bicycle %>
    <% end %>
  </div>
</div>
