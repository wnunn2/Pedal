
<div class="container wrapper">
  <div class="row">
    <div class="col-12">
    <div class="card p-3" >
      <ul class="card-description-list" style="list-style-type:none">
        <li> <strong><%= @bicycle.category %></strong></li>
        <li> <strong>¥<%= @bicycle.day_price %></strong></li>
        <li> <%= @bicycle.description %></li>
        <li> <%= @bicycle.address %></li>
      </ul>
    </div>
  </div>
  </div>

  <div class="row mt-5">
    <div class="col-6">
      <div class="card-trip mt-3 ">
        <% @bicycle.photos.each do |photo| %>
          <%= cl_image_tag photo.key, crop: :fill%>
        <% end %>

        </div>
    </div>
  <div class="col-6">
    <%= render 'bookings/booking-form' %>
    </div>
  </div>
</div>
